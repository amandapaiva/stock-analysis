{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"C:/Users/amand/Downloads/VSCode\" && git add -A && git diff --cached --quiet || (git commit -m \"auto: update project files\" && git push origin master)",
            "timeout": 30,
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
